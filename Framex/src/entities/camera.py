from ..utils.imports import *

# Add different camera times (with options)
# Static Offset (One Position)
# Follow (hard stuck to player)
# Lerp (move slowly towards the player)
# Clamp / Bounded (move around and stop at edges of screen)
# Shake Cam? (Callable during event?)
class Camera:
    def __init__(self):
        pass
